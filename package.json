{
  "name": "merchsy",
  "version": "1.0.0",
  "description": "E-commerce and social media in one.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm start",
    "startapp": "concurrently \"npm run frontend\" \"npm run backend\"",
    "dev": "npm run test && npm run startapp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
